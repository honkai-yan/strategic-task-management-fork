# ===========================================
# 战略指标管理系统 - 环境变量配置模板
# ===========================================
# 复制此文件为 .env 并填入实际值
# cp .env.example .env
#
# ⚠️ 重要提示：
# - 所有 VITE_ 前缀的变量会暴露给前端代码
# - 不要在此文件中存储真实的密钥或密码
# - 生产环境必须配置所有标记为 [必需] 的变量

# ===========================================
# 数据库配置 (用于数据同步脚本)
# ===========================================
# [必需] 数据库主机地址
DB_HOST=localhost

# [必需] 数据库端口
DB_PORT=5432

# [必需] 数据库名称
DB_NAME=strategic

# [必需] 数据库用户名
DB_USER=postgres

# [必需] 数据库密码 - 请使用强密码
DB_PASSWORD=your_secure_password_here

# ===========================================
# JWT 配置 (用于数据同步脚本)
# ===========================================
# [必需] JWT 签名密钥
# 要求：至少 32 个字符的随机字符串
# 生成方式：openssl rand -base64 32
JWT_SECRET=your_jwt_secret_key_at_least_32_chars

# ===========================================
# 服务器配置 (用于数据同步脚本)
# ===========================================
# [可选] 后端服务器端口，默认 8080
SERVER_PORT=8080

# ===========================================
# 前端配置 (Vite 环境变量需要 VITE_ 前缀)
# ===========================================

# -------------------------------------------
# API 配置
# -------------------------------------------
# [可选] API 基础路径，默认 /api
VITE_API_BASE_URL=/api

# -------------------------------------------
# 安全配置 ⚠️ 重要
# -------------------------------------------
# [必需] API 请求签名密钥
# 用于前端生成请求签名，防止请求篡改
# 要求：至少 32 个字符的随机字符串
# 生成方式：openssl rand -base64 32
# ⚠️ 此密钥必须与后端配置的签名验证密钥一致
VITE_API_SECRET=your_api_secret_key_at_least_32_chars

# [必需] 本地存储加密密钥
# 用于加密存储在 localStorage 中的敏感数据
# 要求：至少 32 个字符的随机字符串
# 生成方式：openssl rand -base64 32
VITE_STORAGE_KEY=your_storage_encryption_key_here

# -------------------------------------------
# 功能开关
# -------------------------------------------
# [可选] 强制使用模拟数据 (true/false)
# 当后端服务不可用时，可设置为 true 使用本地模拟数据
VITE_USE_MOCK=false

# [可选] 启用开发工具 (true/false)
# 生产环境应设置为 false
VITE_ENABLE_DEVTOOLS=true

# -------------------------------------------
# 日志配置
# -------------------------------------------
# [可选] 日志级别 (debug/info/warn/error)
# 生产环境建议设置为 warn 或 error
# 开发环境可设置为 debug 或 info
VITE_LOG_LEVEL=info

# -------------------------------------------
# 应用信息
# -------------------------------------------
# [可选] 应用标题
VITE_APP_TITLE=战略指标管理系统

# [可选] 应用版本
VITE_APP_VERSION=1.0.0

# ===========================================
# 环境特定配置说明
# ===========================================
# 
# 开发环境 (.env.development):
#   - VITE_ENABLE_DEVTOOLS=true
#   - VITE_LOG_LEVEL=debug
#   - VITE_USE_MOCK=false (如果有后端) 或 true (无后端)
#
# 生产环境 (.env.production):
#   - VITE_ENABLE_DEVTOOLS=false
#   - VITE_LOG_LEVEL=warn
#   - VITE_USE_MOCK=false
#   - 必须配置真实的 VITE_API_SECRET 和 VITE_STORAGE_KEY
#
# ===========================================
# 安全最佳实践
# ===========================================
#
# 1. 永远不要将 .env 文件提交到版本控制
# 2. 使用强随机密钥（至少 32 字符）
# 3. 定期轮换密钥
# 4. 不同环境使用不同的密钥
# 5. 生产环境密钥应通过安全渠道传递
#
